# Boards

Export the key design boards here as PNGs (one per screen, named after the board, e.g. `S2-ShotList.png`) so Claude Code can see what each screen should look like.

The canvas is the visual reference; `../design.md` is the rulebook. Where they disagree, ask.
